fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary,pool]>=3.2
itsdangerous>=2.2
httpx>=0.27
pydantic>=2.7
python-multipart>=0.0.9
stripe>=15.0,<16
# v0.6: import from a web page (server/importer.py) and PDF/DOCX into the library (server/library.py,
# src/dyslexic_rewrite/io.py). readability-lxml is an optional extraction fallback for importer.py,
# used only when installed -- not required here.
trafilatura>=1.8
pypdf>=4
python-docx>=1.1
